- name: Create multiple users
  hosts: all
  become: yes
  tasks:
    - name: Add users
      user:
        name: "{{ item }}"
        state: present
      loop:
        - alice
        - bob
        - charlie
